# Polymarket BTC 5-min Prediction System
# 复制此文件为 .env 并填入实际值:
#   copy .env.example .env

# ── 钱包私钥 (Polygon EOA) ──
# MetaMask 导出的私钥, 仅实盘模式需要
PM_PRIVATE_KEY=

# ── Polymarket 代理钱包地址 ──
# 连接 MetaMask 后 PM 上显示的那个新地址 (右上角头像 → 复制地址)
# 如果你的 USDC 在 Proxy 钱包里 (大多数用户), 必须填写
PM_PROXY_ADDRESS=

# ── EOA 地址 (可选, 自动从私钥推导) ──
# PM_WALLET_ADDRESS=

# ── 告警 (可选) ──
# TG_BOT_TOKEN=
# TG_CHAT_ID=
# DISCORD_WEBHOOK=

# ── 自定义端点 (可选) ──
# CLOB_HOST=https://clob.polymarket.com
# POLYGON_RPC=https://polygon-rpc.com
